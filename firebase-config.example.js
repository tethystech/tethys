// Copy this file to `firebase-config.js` and replace the placeholder values.
// Do NOT commit your real `firebase-config.js` to public repositories.

export default {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID",
  measurementId: "G-MEASUREMENT_ID"
};

// Example usage in the app (already expected by clientes.html):
// let firebaseConfig = (await import('./firebase-config.js')).default;
// Copy this file to `firebase-config.js` and fill the values.
// Do NOT commit `firebase-config.js` to your repository. It's included in .gitignore.

export default {
  apiKey: "YOUR_API_KEY",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID"
};
